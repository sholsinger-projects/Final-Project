# NutriAI — Automated Diet Plan Builder
**BAX-423 Big Data · Spring 2026 · Final Project**

---

## Tech Stack

| Layer | Tool |
|---|---|
| **UI** | Streamlit |
| **Backend / Logic** | Python 3.11+ |
| **Food Database** | SQLite (populated from USDA FoodData Central API) |
| **Embeddings / Similarity** | FAISS + sentence-transformers |
| **Allergen Filtering** | Bloom filters (`pybloom-live`) |
| **Nutrient Math** | pandas / numpy |
| **Data Source** | USDA FoodData Central API |
| **Clinical References** | Monash FODMAP list, NIH RDA tables |

---

## Setup

### 1. Clone and install dependencies
```bash
cd code/
pip install -r requirements.txt
```

### 2. Get a free USDA API key
Sign up at: https://fdc.nal.usda.gov/api-key-signup.html  
(Instant, no credit card needed)

### 3. Ingest the food database (Phase 1)
```bash
python ingest.py --api-key YOUR_USDA_KEY --target 10000
```
This pulls ≥10,000 food records, tags each one with clinical/allergen flags,
and stores everything in `data/nutriai.db`. It also saves a 5,000-row offline
snapshot to `data/usda_snapshot.csv` for the grader.

> **Offline mode:** If no API key is available, place a pre-built `nutriai.db`
> in `data/` and the app will use it directly.

### 4. Run the app
```bash
streamlit run app.py
```
Then open http://localhost:8501 in your browser.

---

## Project Structure

```
nutriai/
├── code/
│   ├── app.py            # Streamlit UI (Phase 5)
│   ├── ingest.py         # Phase 1: USDA data pipeline
│   ├── db.py             # DB connection + RDA tables
│   ├── filters.py        # Phase 2: clinical & allergen filtering
│   ├── planner.py        # Phase 3: meal plan generation + diversity engine
│   ├── nutrients.py      # Phase 4: macro/micro analysis + RDA flagging
│   ├── embeddings.py     # BAX-423: FAISS food embeddings
│   ├── bloom.py          # BAX-423: Bloom filter allergen checker
│   └── requirements.txt
├── data/
│   ├── nutriai.db        # SQLite food database (generated by ingest.py)
│   └── usda_snapshot.csv # 5,000-row offline snapshot
├── brief/
│   └── brief.pdf         # Technical brief (Phase 6)
├── prompts.md            # AI prompts log
└── README.md
```

---

## Test Personas

The app is pre-tested against 4 clinical personas:

| Persona | Conditions | Diet | Key Constraints |
|---|---|---|---|
| **Priya** | IBS-D | Vegetarian + Lactose-free | Zero high-FODMAP, zero dairy |
| **Ravi** | GERD, Celiac | Non-veg, no pork | Zero GERD triggers, zero gluten |
| **Mei** | Type 2 Diabetes | Vegan | All meals GI ≤ 55, zero tree nuts |
| **James** | Hypertension | Pescatarian | Sodium ≤ 1,500 mg/day, zero soy |

---

## BAX-423 Techniques Used

1. **FAISS Embeddings** — Foods are embedded using sentence-transformers and indexed
   with FAISS for fast semantic similarity search during meal selection and ranking.

2. **Bloom Filters** — A probabilistic Bloom filter is built for each allergen set
   at startup, enabling O(1) average-case allergen safety checks across 10,000+ foods.

---
